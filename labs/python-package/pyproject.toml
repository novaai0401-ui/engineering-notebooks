[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "study-coach-math"
version = "1.0.0"
requires-python = ">=3.11"
[tool.setuptools.packages.find]
where = ["src"]
[tool.setuptools.package-data]
coach_math = ["py.typed"]
